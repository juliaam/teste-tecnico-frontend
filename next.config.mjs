/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['assets.delirec.com', 'conteudo.imguol.com.br', 'assets.unileversolutions.com'],
  },

};

export default nextConfig;
